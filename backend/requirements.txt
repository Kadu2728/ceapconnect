# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- Database ---
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# --- Settings & validation ---
pydantic==2.10.4
pydantic-settings==2.7.1
# Necessário para `pydantic.EmailStr` (validação real de formato de e-mail).
email-validator==2.3.0

# --- Auth (EPIC 02) ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# Pin explícito: passlib 1.7.4 acessa `bcrypt.__about__`, removido no bcrypt
# 4.1+, o que quebra hash/verify em runtime. Manter fixo até migrar para uma
# lib mantida (ex. bcrypt puro ou argon2-cffi) ou o passlib ser atualizado.
bcrypt==4.0.1
python-multipart==0.0.20

# --- Assistente IA (EPIC 11) ---
# Cliente HTTP assíncrono usado para consumir a API do Groq (nível gratuito,
# compatível com OpenAI) em streaming (endpoint `chat/completions`, SSE).
httpx==0.28.1

# --- Predição de evasão (EPIC 14) ---
# Job in-process de recálculo de risco (sem worker/processo separado no Render).
apscheduler==3.11.0

# --- PWA + push notifications (EPIC 18) ---
# Envia notificações push reais via Web Push (protocolo padrão do navegador,
# sem SDK/serviço pago — autenticado com um par de chaves VAPID gerado uma vez).
pywebpush==2.4.0
